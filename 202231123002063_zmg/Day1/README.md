项目简介
本项目提供了一个Python脚本，用于处理TIFF格式的遥感图像，将其转换为真彩色归一化图像。该工具主要针对包含多个波段的TIFF，文件通过提取特定波段（假设为B02、B03、B04，分别对应蓝、绿、红波段），并进行归一化处理，最终生成一个标准的RGB图像，便于后续的可视化和分析。
功能特点
波段提取：从TIFF文件中提取蓝、绿、红三个波段，用于生成真彩色图像。
归一化处理：将提取的RGB波段数据归一化到0-255范围，确保生成的图像具有良好的视觉效果。
图像保存：将处理后的图像保存为PNG格式，方便查看和进一步处理。